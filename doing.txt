Open items, priority order.

1. Fix the pyright-backed tests, then make CI run them. Root cause found:
   commit e7ae496 ("ci: run prek") let prettier reformat
   examples/templates/*.html, which collapsed the multi-line {#def #} headers
   onto one line. The parser then reads the whole header as a single import
   ('from examples.models import User user: User'), so the stub is garbage and
   7 tests in tests/test_checker.py and tests/test_lsp.py fail. The tests are
   correct and the fixtures drifted. Fix: restore the multi-line headers and keep
   prettier off them, either by excluding examples/templates from the prettier
   hook or by renaming the fixtures to .jinja (prettier's types_or does not
   match jinja, and another hook already excludes \.jinja$). Then install
   pyright in the CI test job: the typecheck job runs pyright-action, but the
   macOS/Windows test matrix never installs pyright, so every
   skipif(shutil.which('pyright')) test silently skips. Do this before
   publishing so the tag ships a verified build.

2. Publish to PyPI by pushing a tag. The Publish to PyPI workflow is green and
   tag-gated, and the version is pinned at 0.0.1. No name is reserved on PyPI
   (types-for-jinja and typed-jinja both 404), so the first upload claims the
   name, and pyproject's types_for_jinja normalizes to types-for-jinja.
   Publishing unblocks yak-shears, which dropped its editable path source at
   ../../local-code/typed-jinja to build.

3. Point .copier-answers.yml at gh:KyleKing/calcipy_template. _src_path is
   still the local path /Users/kyleking/Developer/kyleking/calcipy_template,
   which a Fleet run on another machine can't resolve. _commit is already
   5.0.3, so this is a one-line change.

4. Open design question, after publish: yak-shears hand-writes 7 render_*
   functions that restate the parameter list their template's {#def ... #}
   header already declares, the same signature generate_wrapper would emit.
   The blocker is that generate_wrapper returns Markup while yak-shears'
   helpers return starlette.responses.HTMLResponse (and render_error also
   sets an HTTPStatus), and some helpers do real work before rendering.
   Decide whether types-for-jinja grows a "wrap the render in a
   caller-supplied callable" option, or yak-shears keeps its helpers and only
   generates the inner typed render.

Done, for the record: the git remote points at the renamed repo
(KyleKing/types-for-jinja).
