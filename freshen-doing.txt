# freshen follow-ups

## 2026-08-01

Stashed pre-existing uncommitted work as `freshen 672bb50f: pre-existing WIP` (git stash, not dropped, not applied).

Stashed files and best-guess next step:

Follow-ups from the pass:

- 7 tests in tests/test_checker.py and tests/test_lsp.py fail locally and always have. They are gated behind `skipif(shutil.which('pyright') is None)`, and CI never installs pyright into the test job, so CI silently skips the entire pyright-backed suite. The failures look like drift between examples/templates/greeting_bad.html and the assertions (expected 3 diagnostics, got 4; expected `def _render(user: User) -> None:`). Decide whether to update the assertions or the fixtures, then install pyright in the CI test job so the suite actually runs
- `.copier-answers.yml` has `_src_path` on the local path `/Users/kyleking/Developer/kyleking/calcipy_template`. Fleet runs from another machine cannot resolve that. Switch it to `gh:KyleKing/calcipy_template` once 5.0.3 is the published baseline
- typed-jinja is still unpublished, so `yak-shears` had to drop its editable path source at `../../local-code/typed-jinja`. The Publish to PyPI workflow is green and version is pinned at 0.0.1. Publishing a real version would unblock that consumer

Stashed files and best-guess next step:

- PLAN.md — adds one bullet under "Still deferred" about expanding the LSP past the typed context (tag/filter completions, hover docs, completions for the typed context's own keys and attributes, with typed-htmx as the reference). Next step: decide whether that scope belongs in PLAN.md as deferred or as a tracked issue, then restore the line from the stash and commit it.
